mutation CreateUserMutation($input: CreateUserInput!) {
    createUser(createUserInput: $input) {
        user {
            id
            username
            name
            bio
            avatarPublicId
            avatarUrl
            counts {
                followingCount
                followersCount
                postsCount
            }
        }
        token
    }
}
