schema: 'http://localhost:5000/graphql'

generates:
  src/graphql/schema/schema.graphql:
    plugins:
      - schema-ast
    config:
      includeDirectives: true
      commentDescriptions: true
      skipUnusedTypes: false
      sort: false