query CurrentUser {
    currentUser {
        user {
            username
            name
            email
            id
            createdAt
            updatedAt
        }
        token
    }
}
