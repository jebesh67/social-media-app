query CurrentUserQuery {
    currentUserProfile {
        user {
            id
            username
            name
            bio
            avatarUrl
            avatarPublicId
            counts {
                postsCount
                followersCount
                followingCount
            }
        }
    }
}
