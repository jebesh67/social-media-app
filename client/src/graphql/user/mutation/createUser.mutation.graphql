mutation CreateUserMutation($input: CreateUserInput!) {
    createUser(createUserInput: $input) {
        user {
            id
            username
            name
            email
            bio
            avatarPublicId
            avatarUrl
            counts {
                followingCount
                followersCount
                postsCount
            }
        }
        token
    }
}
