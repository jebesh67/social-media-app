mutation LoginUserMutation($input: LoginUserInput!) {
    loginUser(loginUserInput: $input) {
        user {
            id
            username
            name
            email
            bio
            avatarPublicId
            avatarUrl
            counts {
                followingCount
                followersCount
                postsCount
            }
        }
        token
    }
}
