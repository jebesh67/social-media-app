import { CustomHeader } from "@/components/shared/header/CustomHeader.shared";

export const SettingsComponent = () => {
  return (
    <>
      <CustomHeader
        type={ "back" }
        text={ "Settings" } />
    </>
  );
};
