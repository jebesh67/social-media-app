"use client";

import { ProfileCard } from "@/features/profile/main/ProfileCard";

export const Profile = () => {
  
  return (
    <div className={ "w-full pt-8 flex flex-col items-center justify-center" }>
      <ProfileCard />
    </div>
  );
};
