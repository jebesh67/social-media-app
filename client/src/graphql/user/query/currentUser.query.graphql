query CurrentUserQuery {
    currentUserProfile {
        user {
            id
            username
            name
            email
            bio
            avatarUrl
            avatarPublicId
            counts {
                postsCount
                followersCount
                followingCount
            }
        }
    }
}
