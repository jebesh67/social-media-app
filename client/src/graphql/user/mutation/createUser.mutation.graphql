mutation LoginUserMutation($input: CreateUserInput!) {
    createUser(createUserInput: $input) {
        user {
            id
            username
            name
            bio
            counts {
                postsCount
                followersCount
                followingCount
            }
        }
        token
    }
}
