"use client";

import { ProfileCard } from "@/components/profile/ProfileCard";

export const Profile = () => {
  
  return (
    <div className={ "css-body-top-padding w-full flex flex-col items-center justify-center" }>
      <ProfileCard />
    </div>
  );
};
